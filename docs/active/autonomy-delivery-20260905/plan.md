# Plan

## Goal

Improve application throughput, truthful answer/material quality, ATS coverage, and low-intervention execution. Preserve durable job identity, one page writer, SubmissionGate, independent receipts, and manual boundaries for genuine challenges or unsupported material facts.

## Scope

Continue the user's accepted architecture assessment through implementation, focused verification, integrated checks, and measured admission. Main began at 2d1323e; this work starts from the existing, clean local candidate ad81f1c so its eight unmerged commits are preserved. The follow-up explicitly authorizes comparison tasks, local tuning, qualified real applications, merge and push to the personal fork. Root owns integration and real side effects; cleanup of unrelated worktrees remains outside scope.

## Sources of truth

- Current source and tests in this worktree.
- Prior candidate records at docs/active/application-orchestration-hardening-20260829/.
- Independent audit task 01a06f79-281c-7482-b7c6-8bd683451565.
- Existing operational wrapper, immutable materials, authority state, and actual receipts for any authorized live canary.

## Stages

1. Repair Supervisor waiting/progress semantics and unify current-profile material admission before browser allocation.
2. Deliver one bounded ATS routine-field fast path and a real bounded exception/specialist execution path using existing contracts.
3. Align resolved model/effort and provenance across CLI/App Server; qualify persistent runtime/transport prepare behavior before any authoritative promotion.
4. Make small-batch progress and restart guidance actionable for the user's normal 5-10-job runs. Reuse the durable ledger and single-job prompt; avoid a new large-batch scheduler.
5. Persist reusable, value-free experience candidates with validation and invalidation, and qualify promotion/fallback behavior. Do not infer product gains from module availability alone.
6. Compare runtime event streams, 5/10-job restart/material cases, and representative ATS browser DOM using isolated tasks; integrate only fixes supported by failures.
7. Qualify a small real application cohort through the operational wrapper, reconcile exact outcomes, verify release packaging, then merge and push the candidate and check remote CI.

## Acceptance criteria

- Normal startup, reasoning and bounded tool waits do not cause premature park; repeated ineffective actions and actual deadlines remain bounded.
- Candidate readiness and final runtime checks agree on current material facts; read-only queries stay read-only.
- Every new execution slice has a real caller and meaningful behavior tests; unsupported state never becomes success.
- Model/effort comparisons use resolved configuration and matched work, with quality and receipt evidence kept separate from timing.
- Batch restarts preserve every job's status and do not repeat unknown or completed effects; parent and worker context contain bounded references rather than all job transcripts.
- No concurrency or recipe promotion without the relevant execution, isolation and quality evidence.

## Latest user constraint

The user clarified that 100-job runs are rare; 5-10 jobs per run are sufficient and the budget should stay modest. Use fixture comparisons and a small live cohort; do not resubmit the same job for A/B. There is no controlled paid model-effort benchmark in this wave. App Server browser-write authority and worker concurrency are not expanded. Report real case outcomes separately from local contract evidence and avoid population throughput claims from a few jobs.

## Non-goals

No wholesale orchestration-framework migration, profile invention, global lowering of reasoning, authority expansion based only on a feature flag, production deployment, force push, or cleanup of prior worktrees.

## Risks and constraints

Launcher is a shared integration hotspot: Supervisor lane owns its edits first; runtime and readiness lanes supply other launcher wiring to root. Worker tasks use isolated worktrees and focused tests. Root alone owns full tests, builds, browsers, model/MCP processes, live profiles and final integration.

Latest company selection: prioritize employers with no prior local attempts/receipts/applied state, even where a new role at an old employer has no exact-job receipt. This run qualified Temasek, Allium, Unravel Carbon and Syfe at company level; do not repeat old employers to manufacture comparison samples.
