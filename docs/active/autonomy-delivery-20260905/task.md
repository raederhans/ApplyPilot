# Small-batch autonomy delivery

## Scope

The user authorized comparison tasks, evidence-based local tuning, qualified real applications, merge and push. Normal batches are 5-10 jobs with a modest experiment budget. The latest instruction excludes previously attempted employers from the real cohort. Root owns integration, real data/browser/Submit, combined checks and Git delivery; delegated tests use isolated resources.

## Delivered behavior

- Supervisor separates legal waits from repeated no-progress and counts one tool-call lifecycle once, while retaining deadlines and effect-unknown stops.
- CLI/App Server share resolved model/effort and provenance. Defaults remain Sol/high; no new production App Server write authority is enabled.
- Material admission reads the actual upload PDF; contradictory sidecars cannot override it, and unextractable text stays unknown.
- Bounded routine filling supports ordinary Workday/SmartRecruiters fields. Default off; canary retains the second live audit and SubmissionGate. Country aliasing requires an exact ordinary-country label.
- Small-batch next/progress uses durable state and bounded references. Stored recipe experience is value-free, validated and observational.
- Preferred job skills no longer become hard gaps merely because a broad experience marker appears earlier. Temasek qualifies using the same factual profile and materials.
- The official SuccessFactors European suffix is recognized by credential relay; other capabilities and root/page/password/Submit restrictions remain unchanged.

## Comparison evidence

Details: [runtime](comparison-runtime.md), [ATS](comparison-ats.md), [batch/material](comparison-batch.md), [new companies](comparison-new-companies.md).

Supervisor fixtures: false interventions 11 to 0 across three legal-wait scenarios; repeated no-progress and effect-unknown outcomes stay bounded. Isolated real-DOM fixtures: verified routine filling avoids the initial prepare Agent, with drift and unsupported controls failing closed. Five/ten-job acquisition keeps one material predicate read for the same first eligible job; the optional snapshot costs five/ten reads. These are controlled local measurements, not production throughput gains.

## Verification and release

Non-browser regression after the initial parser repair: 2267 passed, 47 deselected in 104.42s. The earlier two invalid-PDF legacy fixtures now use parseable PDFs and pass. ATS affected checks including disposable Chromium: 68 passed in 20.47s. EU-provider checks: 27 passed in 0.53s. Groups overlap and must not be added as unique test counts.

Ruff and diff checks passed. Release build/privacy audit and clean installed-wheel smoke passed before the final provider/parser follow-ups. The mixed-clause parser review finding is resolved; 40 affected tests and a focused review passed. The whole browser tier passed 53 tests; final build/privacy audit passed. The project-owned Python MCP tools now explicitly receive an existing parent PYTHONPATH, preventing mixed-source canaries; unrelated tool environments are unchanged. Final affected runtime/identity/provider checks: 82 passed in 10.90s; Ruff and the final release rebuild/privacy audit passed.

Delivery code commit: 18dcb0bfe614fbc581c93686a4e11e1afcfa229e, including implementation baseline a7cb610 and ancestor candidate ad81f1c. Root fast-forwarded clean main from 2d1323e and pushed to personal fork raederhans/ApplyPilot; ls-remote confirmed the exact code commit. This documentation follow-up records that observed delivery. Current remote verification is available in the [main CI runs](https://github.com/raederhans/ApplyPilot/actions/workflows/ci.yml?query=branch%3Amain); the parent task reports the final observed CI outcome.

## Real applications and limits

New-company qualification covers Temasek, Allium, Unravel Carbon and Syfe. NCS was stopped during prepare on the user's steering, with submit_started=0 and no exact-job receipt. Temasek first reached login_issue on an unrecognized SuccessFactors EU host; root ended only that pre-submit attempt through the ledger API, followed by normal worker/browser cleanup. No human response was invented. One bounded retry also reached login_issue and was ended pre-submit. Later source-path inspection showed the parent-only override had not established matching helper versions; these live attempts therefore do not qualify the updated end-to-end relay. All four new-company receipt counts remain zero.

Allium and Syfe expose missing subtype coverage; Unravel Carbon lacks registered evidence for a named required skill. None was force-admitted. Success requires a verified receipt.

Successful applications per hour, tokens per verified application, broad ATS autonomous completion and online model-effort comparisons remain unproven. Different jobs and an interrupted run are not a controlled speed comparison. Multi-cell production writes and experience-driven autonomous writes remain gated. Worktrees are retained for recovery; unrelated WIP is preserved.
