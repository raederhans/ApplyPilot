# Task

## Current status

The P0/P1/M3 implementation slices are complete locally. The six-application multi-channel live wave is paused at the user's 2026-08-30 stop request under `/root` as the sole browser/database/Submit owner. Five new exact applications are receipt-confirmed and reconciled: ShopBack Product Management (Lever), ShopBack Backend Engineering (Lever), Simular AI Product Manager (Ashby), Grab OmniCommerce (SmartRecruiters), and NCS AI Engineer Intern (SmartRecruiters). GSK Digital Data Analytics is prepared at fit `8` with a validated resume but is paused before Workday account creation; its live cover-letter requirement has not yet been observed. NCS Business & Strategy Analyst is a fit-`8`, coverage-`1.0`, machine-validated fallback. ShopBack Business Analyst and Operations Automation are historical duplicates and do not count. Only new exact receipts count toward six.

The audit continuation remains based on `12d20b6ad4dc57f10e85974e8240d3c203944d43`; evidence collection and implementation continued on 2026-09-01. Current GitHub Actions run `33395218637` is invalid as an integration baseline because it failed with zero scheduled jobs. P0 is complete locally. P1 durable storage/runtime/browser ownership, P2 semantic Workday/SmartRecruiters writes, P3 typed accuracy/provenance contracts, P4 Exception Operator Center, and P5 replay/local-Chromium benchmark instrumentation have passed their local specialist gates and independent review. The P6 isolated local candidate passed full Ruff, the one authorized full pytest run, audited build, clean-wheel install smoke, workflow expansion, and migration/backup-restore. P6 remains open only for an authorized commit/push and a non-zero four-job remote CI run on that candidate. The prior `5/6` live wave remains paused until remote qualification reaches the live-canary promotion gate.

## Checklist

- [x] Define and enforce application-surface taxonomy with LinkedIn entry, native, and verified external routes.
- [x] Share one frozen-candidate admission decision between authorization and acquisition.
- [x] Report requested, executable, blocked, and effective workers.
- [x] Reject missing materials and exhausted attempts before Agent/browser startup.
- [x] Classify post-submit evidence independently of filenames and Agent assertions, including receipt/duplicate conflicts.
- [x] Implement one bounded production specialist and deterministic reducer.
- [x] Persist journal/idempotency/replay and proposal lifecycle telemetry.
- [x] Add resource-aware scheduling, final-submit serialization, and safe MCP startup reductions.
- [x] Run focused regression and shared-contract validation.
- [x] Run a controlled two-worker fill-only dry-run under one coordinator; no batch reservation or receipt was produced.
- [x] Run supervised real application attempts with receipt accounting; HP `UNI4131` produced one target-ATS-confirmed, locally reconciled browser receipt.
- [x] Recognize a same-job LinkedIn authwall before Apply without treating it as an application URL or ATS handoff.
- [x] Dismiss the exact non-application LinkedIn app promo and revalidate target/root/job immediately before Apply.
- [x] Add bounded acquisition, submit-lane, Agent, and observer performance metrics with thread-safe run aggregation.
- [x] Split ordinary ATS sign-in, credential relay, account creation, and existing Google-session reuse into separately auditable authentication capabilities; persist the user's standing login authorization without weakening material-answer or submit gates.
- [x] Harden Workday segmented-date guidance and localized boolean resolution after the connected-browser form exposed both failures.
- [x] Record the HP live-run rule that target-ATS state and durable receipts outrank third-party extension status; no extension status is application proof.
- [x] Record exact-scope user-confirmed `No` answers for the HP government-employment and conflict/government-influence questions, without extrapolating them to other screening questions.
- [x] Require calendar interaction when a Workday segmented/composite date control exposes a calendar; the typed fallback corrupted a live year value before calendar recovery.
- [x] Record standing authorization for routine final Submit after all exact-job/material/preflight/submission gates pass, without weakening CAPTCHA/assessment/identity/financial/unsupported-answer/submission-uncertain stops.
- [ ] Reconcile uncertain/duplicate outcomes without counting false success.
- [x] Complete the P0/P1 integration handoff in the sole registered `main` worktree, with no unrelated WIP overwritten and no `origin` mutation.
- [x] Confirm the P0 patch on remote Python 3.11/3.12/3.13 CI. GitHub Actions run `33304564021` passed all three Python jobs and Windows clean-install smoke.
- [x] Add and test a pure Application Actor transition kernel with typed autonomy decisions, human interruptions, bounded recovery actions, and schema-v1 durable parsing.
- [x] Connect bounded non-submit production paths to the new Actor/Policy/Recovery result and prove through real `run_job` and worker entries that the result is consumed; no dead import, default-off no-op flag, or log-only scaffold is accepted.
- [x] Prove the two slices compose without duplicating existing events, checkpoints, task journal, human requests, failure taxonomy, or submission authority.
- [ ] Define replay comparison for the first prepare/recover production migration; do not wire it until P0 is green.
- [x] Add stable `actor_id` / per-turn `turn_id`, backward-readable DecisionEnvelope v2, actor-scoped checkpoint CAS and deterministic completed-turn replay through the real launcher/control plane.
- [x] Prove stale actor sequence rejection, same-event replay no-op, v1 upcast, HUMAN_ONLY replay and crash-after-commit retry through focused integration tests; caller-owned transactions use SAVEPOINT rollback so partial control writes cannot escape.
- [ ] Run the full core suite on Windows Python 3.12 in CI and execute the installed `applypilot resume-route --help` smoke without weakening the stable metadata unit test. The workflow and local Windows proof are complete (`947 passed` plus clean-install CLI smoke); remote `windows-latest` remains pending until an authorized push.
- [ ] Design provider-neutral session and browser-profile leases plus high-level page-versioned operations after Actor replay parity exists.
- [ ] Run matched throughput cohorts only after session/profile continuity and exception parking are implemented; retain two-worker production cap meanwhile.
- [x] Freeze a six-job multi-channel queue from the current library after applied/uncertain/duplicate/attempt/material exclusion.
- [x] Prepare and machine-validate the resume materials needed for the six statically admitted exact jobs; cover/readiness discovery remains a runtime pre-submit condition.
- [x] Record the exact MCAAI, UPenn, and UCL education start months and consume them in the browser Agent prompt.
- [x] Encode optional-versus-required NTU and programme-credit/placement proof policy, and keep optional transcript evidence out of the deterministic material block.
- [x] Add a local DPAPI-protected FIN relay with required-only field matching, post-fill persistence checks, masking, durable-state redaction, and no Submit authority.
- [x] Record scikit-learn, TensorFlow, PyTorch, experimental design, and A/B testing only as low-weight ongoing keywords with explicit no-completed/no-production constraints.
- [x] Make speculative cover-letter generation opt-in for the full pipeline while preserving explicit generation and runtime ATS requirement discovery; never regenerate jobs already observed as `not_required`.
- [x] Add explicit LinkedIn Applied full/incremental import semantics so one verified baseline can feed a cumulative exclusion ledger, while incremental patches cannot impersonate complete radar coverage.
- [ ] Submit exact jobs serially with one live owner and reconcile each decisive receipt before starting the next.
- [ ] Close only when six new receipt-confirmed applications are recorded, or document the exact hard-gate exhaustion without counting blocked/uncertain attempts.
- [x] P0 local: remove the job-level `runner.temp` expression, bind the Windows workspace after runner startup, and enforce the exact four-job CI contract locally.
- [ ] P0 remote: on the candidate revision, prove all four expected jobs are created and green; separately configure required checks only with explicit repository-governance authorization.
- [x] P1: implement durable browser/profile leases and runtime turns with migration, CAS, process-restart, stale-epoch, and two-process contention tests.
- [x] P2: implement page-versioned semantic writes and Workday/SmartRecruiters adapters with synthetic ATS, replay, upload-postcondition, stale-write, and SubmitGate tests.
- [x] P3: replace authoritative string failures and generic first-option fallback with typed failures, scoped facts, risk policy, provenance, and golden-corpus accuracy tests.
- [x] P4: make the exception queue operable through a typed local command plane with resolve/resume/reconcile, grouping-scope, circuit-breaker, and batch-continuation tests.
- [x] P5: establish replay and local-synthetic benchmark instrumentation plus matched 1/2/4 dry cohorts before any worker-cap promotion; retain live-provider measurement for the post-P6 supervised canaries.
- [ ] P6: local candidate passed the isolated full suite, Ruff, build/clean-install, migration/backup-restore, and workflow validation; non-zero-job remote CI on the same candidate remains pending explicit commit/push authority.
- [ ] Final live programme: after P0-P6, run multiple serial supervised end-to-end applications under `/root`, reconcile every exact receipt before the next case, and record quality/performance metrics and all hard stops.

## Validation evidence

| Command or check | Result |
| --- | --- |
| P3 final specialist gate | Root reruns passed: provenance/MCP/worker focused `48 passed`, typed-accuracy composition `333 passed`, compatibility composition `305 passed`, and scoped Ruff `All checks passed`. Four independent review rounds closed option truncation, host-risk/fact authority, optional/overflow denominators, and real worker two-turn repair propagation; final verdict `ACCEPT`. No live browser, default database, provider, Submit, receipt, commit, push, or remote action was used. |
| P4 final specialist gate | Typed CLI/operator lifecycle, exact receipt-only reconcile, durable requested resume, cross-process replay, same-process prepare-only dispatch, bounded heartbeat/timeout/stop/lease-loss expiry, and worker ordering passed. Root reruns: `195 passed` across control/runtime/operator slices and `151 passed` for the worker contract; dispatcher deadline/stop race coverage `6 passed`; scoped Ruff and diff checks passed. Second independent review returned `ACCEPT`. No live browser, default database, provider, Submit, receipt, commit, push, or remote action was used. |
| P5 final specialist gate | Root composition passed `31` tests across deterministic replay, run-progress, matched 1/2/4 cohorts, and real local headless Chromium. The CLI used fixture SHA `1ff017b89222096cb3fdf3d42853a2573c8c7d68bc1afecb1da0e9788c92caab`, observed runtime-run peaks `1/2/4`, exactly-once work, and zero decision drift. Eight offline DOM scenarios used fresh contexts/pages and abort-all networking; four active Submit traps failed closed. Scoped Ruff passed. Independent review replayed the prior output-overwrite, non-zero-side-effect-reset, false-overlap, broken-barrier, and Chromium safety cases and returned `ACCEPT`. Evidence is explicitly replay/local-synthetic only, `promotion_eligible=false`; no default database, live profile, provider, Submit, receipt, commit, push, or remote action was used. |
| P6 isolated local qualification | Candidate `C:\Users\raede\AppData\Local\Temp\applypilot-p6-candidate-20260901-01` starts at `12d20b6ad4dc57f10e85974e8240d3c203944d43` and contains 81 task-owned files with zero byte mismatch against the integration workspace; the three protected scoring files and `tmp/` have zero candidate changes. Full Ruff passed. The single full-suite run passed `1529` tests in `77.35s`. Release audit produced exactly `applypilot_local-0.4.0-py3-none-any.whl`, `applypilot_local-0.4.0.tar.gz`, `applypilot-local-0.4.0-bundle.zip`, and `SHA256SUMS`; all three recorded artifact hashes matched. Clean install passed resume-route help, version, init, doctor, and dashboard without creating its workspace database. Legacy migration expanded the jobs table to `66` columns, preserved and converted the retry sentinel, created all required control/semantic tables, survived SQLite backup/restore, and remained identical after a second `init_db`; migrated/backup/restored integrity were `ok`, foreign-key violations `0`, and no default DB was created. Workflow validation exposed exactly Python 3.11/3.12/3.13 plus Windows 3.12 smoke with no job-level runner-context violation. No provider, live profile/database, Submit, receipt, commit, push, or remote action occurred. |
| Initial worktree status | Clean at `d53ae6e` |
| Isolated-worktree focused orchestration/runtime/submission baseline | `118 passed in 5.27s` at `d53ae6e` |
| Historical duplicate evidence tests | `6 passed`; positive receipt, duplicate wording, uncertain form, no retry/repair/count, and neutral screenshot coverage |
| Runtime contract after evidence change | `84 passed`; combined evidence/runtime run `90 passed in 2.83s` |
| Package registry check | Playwright MCP `0.0.79`; Gmail MCP `1.1.11` |
| Independent evidence review | `REQUEST CHANGES`; reproduced receipt-vs-duplicate conflict and non-success retention metadata misclassification |
| Policy/admission hardening | `243 passed`; Ruff passed; committed as `da0f410` after canonical policy commit `75d3a6f` |
| Orchestration focused suite | `48 passed in 3.68s` |
| Material/evidence regression | `33 passed, 72 deselected` |
| Admission/gate/worker regression | `28 passed, 78 deselected` |
| Broad shared-contract suite | `407 passed in 17.05s` after updating schema expectations for the two durable journal tables |
| Touched Python lint | Ruff `All checks passed` |
| Patch integrity | `git diff --check` exit 0; only checkout line-ending warnings |
| Live status semantics | Raw prepared `8`; canonical admission `2` after HP/Shopee material projection; CLI now shows both instead of calling the raw value ready |
| Infineon interrupted test attempt | Recovered `1` pre-submit attempt, `0` submission-uncertain; `submit_started=0`, no gate/consumption/receipt, no application counted |
| HP LinkedIn-to-Workday preparation | Automatic `reuse_exact`, required coverage `1.0`, score `0.957143`; projected machine-validated artifact |
| Shopee LinkedIn-to-official preparation | Explicit qualified tie selection, required coverage `1.0`, score `0.95`; projected the matching product-management artifact |
| Keppel preparation gate | Blocked: required named skill `machine learning` unsupported; no projection or browser run |
| Grab strict revalidation | Blocked as `failed_judge` for unsupported/inflated claims; same byte content must be invalidated in the reusable artifact library |
| Isolated full regression | `744 passed in 24.84s` with `APPLYPILOT_DIR=C:\Users\raede\AppData\Local\Temp\applypilot-tests-20260829-p0p2` |
| Final isolated full regression | `876 passed in 54.51s` with the dedicated test data root; full Ruff passed; `git diff --check` passed with line-ending warnings only. |
| Full Ruff and patch integrity | Ruff `All checks passed`; `git diff --check` passed with line-ending warnings only |
| HP live LinkedIn entry test | LinkedIn Apply reached HP Workday, credential relay ran, and resume upload was attempted twice; no submit gate or receipt. Failed pre-submit after 228 seconds with `resume_upload`, showing early external handoff persistence currently occurs too late. |
| LinkedIn three-path and early-handoff checkpoint | `linkedin_apply_entry` resolves through native Easy Apply or `linkedin_to_official_ats`; prompt emits the early `LINKEDIN_EXTERNAL_HANDOFF` marker, worker observes the bound external page, sanitizes/rebinds the URL, releases the stale attempt, and does not reserve or count a submission. |
| Codex App Browser login bridge | App Browser reaches the LinkedIn login page, but no supported session/tool bridge currently transfers that authenticated state into the isolated Playwright/Codex worker; login continuity remains manual-review blocked. |
| Controlled two-worker dry-run | One coordinator exercised two isolated preparation workers with final-submit capacity one. It used fill-only preview semantics, created no batch reservation, and produced `0` receipt-confirmed applications; the run was slower than the single-worker baseline. |
| Fill-only preview contract | Preview can populate and inspect the real form but cannot click final Submit/Send/Finish or send email. The launcher restores preview state and keeps `previewed`, authorization, final action, and receipt-confirmed counts distinct. |
| Runtime performance telemetry | Per-turn evidence captures `process_spawn_ms`, `turn_setup_ms`, `prompt_build_ms`, `first_output_ms`, `first_tool_ms`, `last_tool_ms`, `tool_call_count`, and `unique_tool_count`; these are the comparison fields for future worker-scaling tests. |
| LinkedIn authwall/app-promo hardening | `34 passed`; post-Dismiss job-drift regression fails closed before login inspection, Apply, or attestation; independent re-review `ACCEPT`. |
| Focused performance-observation regression | `344 passed in 24.30s`; touched Ruff `All checks passed`. Empty/acquired attempts have separate counts, and a controlled clock proves two lane-hold segments total exactly once before terminal-attempt metrics are backfilled. |
| Independent performance-observation re-review | `ACCEPT`; acquired/empty/blocked/error attempts are separately observable, terminal lane metrics retain the complete hold, and telemetry remains advisory to authorization, receipt admission, and single-writer decisions. |
| Isolated v3/v4 login tests | Both reached the expected login-only surface and stopped at Google identifier with `LOGIN_ISSUE`; neither clicked a second Apply, filled an ATS form, submitted, or created a receipt. |
| Connected Edge route proof | Signed-in LinkedIn job `4455274411` opened exact HP Workday requisition `UNI4131-1`; this proved the company-site path before the later authenticated continuation. |
| Standing login authorization rule | Private runtime profile enables ordinary ATS sign-in, credential relay, account creation, and existing Google-session reuse. Versioned example defaults all four capabilities to `false`; explicit new keys override the legacy account-creation fallback. Final focused compatibility suite: `179 passed in 16.12s`; touched Ruff and `git diff --check` passed. |
| Connected Edge HP continuation | Existing Workday credentials were reused without exposing the password. Steps 1 and 2 were saved, exact resume artifact `resume-9a6170ac3be39d3ad0cdc849.pdf` was uploaded, and step 3 retained only evidence-backed answers. No final submit or receipt occurred. |
| Workday date/localization hardening | Bulk fill now explicitly excludes Workday segmented/composite dates; calendar or verified per-segment entry owns them. Boolean resolution recognizes bounded Chinese labels such as `是`, `否`, `不是`, `同意`, `不同意`, and `不适用` while preserving the visible option and the confirmed-fact gate. Combined focused suites: `212 passed in 14.36s`; touched Ruff and `git diff --check` passed. |
| HP supervised continuation | Official HP ATS showed `UNI4131-1` as an unsubmitted draft while Simplify claimed an Aug 30 application; the official state governs and no receipt exists. Edge restored the existing login through saved credentials without password inspection. Steps 3-4 used the user's exact-scope government-employment/conflict confirmations, then the browser reached 6/6 Review; no final Submit was clicked. |
| Workday segmented-date live evidence | Per-segment typed fallback lost focus and corrupted the graduation year to `2031`; calendar navigation restored the intended value. Where the control exposes a calendar, use it exclusively and verify the visible result before continuing. |
| HP final submission and receipt reconciliation | Submit clicked exactly once. HP Workday showed `已提交申请` / `您的申请已成功提交`, routed to `/jobTasks/completed/application`, and listed `UNI4131` as `In Progress`. `reconcile-receipts` changed the exact LinkedIn job row to `applied` with evidence `browser_receipt:hp-workday-UNI4131-completed-application-20260830T1029+0800` and confidence `durable_receipt_reconciled`. |
| Current remote CI baseline | GitHub Actions run `33296366807`: Windows clean-install smoke passed; Python 3.11, 3.12, and 3.13 each failed the same three tests with `3 failed, 902 passed`. |
| Remote CI closeout | GitHub Actions run `33304564021` on `d9d67129e703ee4ffee6d4236720e7af23ec7dac`: Python 3.11, 3.12, 3.13 and Windows clean-install smoke all passed. |
| Shared-chat extraction and repository reconciliation | Full assistant proposal was read from the exact user-shared Edge tab. Local main shows several recommended primitives already implemented, so the upgrade plan is incremental rather than a wholesale rewrite. |
| Parallel execution dispatch | P0 task `01a051b6-3da7-7db1-9a4c-379847e5c34d` and Actor vertical-slice task `01a051b6-6fef-7d71-88a3-2836d602f2ec` completed with no commit/push/live-run authority. |
| P0 isolated baseline proof | Starting from `1da96ad`, applying only the three P0 test patches produced `905 passed in 34.22s`; full Ruff passed. Additional probes passed without `cloakbrowser`, without Codex on `PATH`, and under Typer `0.27.2`. |
| Actor production-consumption gate | `106 passed` over pure actor, real `run_job`, worker fallback, submit-once, receipt uncertainty, control-plane and HUMAN_ONLY cases; scoped Ruff and `git diff --check` passed. |
| Final combined working tree | Pre-push rerun `.venv\\Scripts\\python.exe -m pytest -q`: `947 passed in 31.57s`; `.venv\\Scripts\\python.exe -m ruff check .`: `All checks passed!`; `git diff --check`: exit 0 with line-ending warnings only. |
| M3 durable Actor focused gate | `65 passed`; includes real `run_job` v2 identity/CAS persistence, two-turn actor sequence, completion and HUMAN_ONLY replay, stale CAS, caller-owned transaction SAVEPOINT rollback, and v1 read-only migration. Scoped Ruff and diff checks passed; independent review verdict `PASS WITH NOTES`. |
| M3 Windows truth gate | Local Windows Python 3.12 full core suite `947 passed in 30.80s`; isolated wheel smoke called the installed `applypilot.exe resume-route --help`, then init/doctor/dashboard. Remote `windows-latest` remains pending until an authorized push. |
| M3 final integration gate | Isolated full core suite `955 passed in 32.12s`; full Ruff, workflow YAML parse and `git diff --check` passed. A final wheel built from the integrated tree and the clean-install smoke passed through installed CLI help, init, doctor and dashboard. |
| Six-channel live receipt audit | ShopBack Product Management, ShopBack Backend Engineering, and Simular AI Product Manager each have a distinct target-confirmed browser/ATS receipt reconciled to the exact database row; current count is `3/6`. CAPTCHA, identity, unsupported-answer, upload, and timing/letter blockers were not counted. |
| Simular visual-browser submission | Ashby showed `Success` and `Your application was successfully submitted`; receipt `browser-9d2ada4f-abb7-4ad2-814d-0b9cef981009` reconciled the exact job to `applied`. A deterministic required-field validation was repaired before the single final Submit; no uncertain duplicate retry occurred. |
| Confirmed-skill router regression | `tests/test_resume_library.py`: `28 passed in 4.64s`; scoped Ruff passed. Only positive `user_confirmed` `*_experience_years` facts for known/configured skills satisfy the unsupported-skill gate, their provenance is recorded, and they do not inflate reuse coverage. |
| Grab OmniCommerce preparation | The production route moved from `manual_review` to `create_variant` without changing the pre-tailoring `0.666667` coverage, then to `reuse_exact` after exact-job validation. Artifact `resume:15ca52a0cbfa2ff6835f89ac` renders as a readable two-page PDF and readiness is confirmed; browser upload and submission remain pending. |
| Grab OmniCommerce visual submission | SmartRecruiters showed `Application submitted!` and the exact-success sentence for Grab `Intern, OmniCommerce`. Receipt `browser-d6aece97-6474-42a7-ad4b-1497bbb608b2` reconciled the canonical job to `applied`; the first receipt ingest rejected an incomplete transcription, the receipt was corrected to include the visible success heading, and no second Submit occurred. Current count is `4/6`. |
| ShopBack Operations Automation duplicate check | The visual Lever form accepted the validated resume and truthful fields, then returned `Your application was already submitted` with provider date `August 24, 2026`. The exact job is now permanently `already_applied`; no new receipt was created and the wave remains `4/6`. |
| ShopBack Business Analyst duplicate check | The official Lever identity maps to an earlier application dated `August 26, 2026`. The exact job is permanently `already_applied`, retry-blocked, and uncounted. The check exposed a production dedupe gap: public Lever, `/apply`, and `lever-source=LinkedIn` URL variants are not yet normalized to one identity. |
| NCS AI identity and material gate | The initial import confused the public numeric job ID with the SmartRecruiters publication UUID. Visual-browser evidence corrected the application/canonical URL to publication `4522844c-7ed4-4f54-ac5b-98d4165f2492`; route assignment `7568b865-33dd-4fad-bf13-efa440e82f86`, coverage `1.0`, artifact `resume:0772a8991295d2af7029bf04`, and readiness fingerprint `20032a85238a298e5bb4e7193cb30b2ae542c5c7a92594adb70279162b3524c5` were then recomputed and consumed. |
| NCS AI visual submission | SmartRecruiters accepted one final Submit and showed `Application submitted!` plus the exact NCS success sentence. Receipt `browser-21653a79-ecbf-406b-90dc-2ab048cd0d67` reconciled the exact canonical job to `applied` with `durable_receipt_reconciled`; no second Submit occurred. Current count is `5/6`. |
| SmartRecruiters live-component findings | Resume parsing populated experience and education but overwrote the legal first name and cleared contact fields; root reapplied verified profile fields after upload. Mouse option clicks closed custom selects without committing values; keyboard selection plus a second DOM audit cleared six false-empty required fields. Multi-select required explicit keyboard focus/Enter handling. |
| GSK Workday pause point | Official req `444672` is current, fit `8`, eligible, and has coverage `1.0` with artifact `resume:9a6170ac3be39d3ad0cdc849`. Production material readiness remains `needs_review` because cover-letter necessity is unknown. Visual Workday reached step `1/7` (`Create Account/Sign In`) and was stopped before account creation at the user's request; no GSK account, application attempt, Submit, or receipt was created. |
| NCS Business & Strategy fallback | Official SmartRecruiters publication `9a04b9a9-424f-4afc-a4c9-5eedc493b048` is eligible at fit `8`; machine-validated artifact `resume:ca41dbb0cacd9df949689af1`, coverage `1.0`, and current-fingerprint readiness are stored. No ATS form was opened and no submission or receipt exists. |
| Education/material/identity focused gate | `31 passed, 117 deselected in 3.89s`; the production profile renders all three education start ranges, optional evidence stays blank, required unavailable evidence skips, and the FIN relay is required-field-only and non-submitting. |
| Shared local compatibility gate after profile/relay changes | `182 passed in 12.07s`; Agent runtime, prompt, MCP registration, page observation, and report-contract behavior remain compatible. |
| Actor/control-plane regression after shared-contract redaction | `46 passed in 2.49s`; actor, control-plane, and replay paths remained green after protected-identifier persistence rejection was added. |
| Production profile and local credential checks | `production-profile-consumption-ok` after confirming the ongoing framework/experiment terms are Agent-consumed application facts but absent from the cover/tailor/validator skills boundary. The encrypted record ownership/ACL check returned one current-user access rule, and an in-memory DPAPI format probe passed without printing or persisting the decrypted identifier. |
| FIN exact-binding and secret-boundary gate | `15 passed` over context digest, attempt/application identity, CDP root/opener lineage, exact path/query/provider binding, pre-decrypt required-field uniqueness, one-shot decryption, post-fill masking, and fail-closed clearing. The combined identity/profile/ATS-tool gate passed `29` tests. |
| Independent FIN security re-review | `PASS`, risk `LOW`; no remaining confirmed vulnerability in scope. Unknown/token query values are not Agent-visible, durable reports reject embedded identity numbers, and optional/missing/ambiguous FIN fields never reach DPAPI. |
| Final profile/material/identity full gate | Isolated full suite `990 passed in 34.32s`; full Ruff returned `All checks passed!`; `git diff --check` exited `0` with line-ending warnings only. |
| Cover/LinkedIn policy integration gate | `108 passed in 9.06s` across radar snapshot/import, cover pipeline, streaming backpressure, material readiness, application decision, and submission admission tests. Scoped Ruff passed and `git diff --check` exited `0` with line-ending warnings only. |
| Production consumer checks | The private profile parses with `cover_letter.auto_generate_in_full_pipeline=false` and `allow_runtime_cover_letter_discovery=true`; the production policy loader returns `False`. Incremental Applied imports update the cumulative `jobs` exclusion set but remain `integrity_valid=false` for report publication. |
| Independent LinkedIn incremental review | Initial `REQUEST CHANGES` found an unbound source namespace; after pre-write `linkedin*` validation and a no-write regression, final verdict `PASS`. Full, incremental, duplicate-ID, missing-base, wrong-source, and exact-snapshot gates remained fail-closed. |
| Live visual LinkedIn read-only check | The signed-in Tracker showed `Applied · 73`; eight visible pages yielded `10,10,10,10,10,10,10,3` records and `73` unique LinkedIn job IDs before the Next control disappeared. No application, note, status, file, or local ledger was changed. |
| 2026-08-31 continuation baseline | Local HEAD and `fork/main` are `12d20b6ad4dc57f10e85974e8240d3c203944d43`. `gh run view 33395218637` reports `conclusion=failure`, the same head SHA, and `jobs=[]`. |
| Current repository-governance state | Read-only branch-protection query returned `Branch not protected` for `fork/main`; no setting was changed. Required-check configuration remains an explicit external action. |
| Protected parent worktree state | Pre-existing `M` changes in `src/applypilot/scoring/pdf.py`, `tailor.py`, and `validator.py`, plus untracked `tmp/`, are outside this continuation's ownership and remain untouched. |
| P0 local workflow gate | `.\.venv\Scripts\python.exe -m pytest -q tests\test_ci_workflow_contract.py`: `2 passed in 0.08s`. The contract parses every workflow, rejects job-level `runner.*`, locks the two CI job IDs/four check names, and verifies the Windows `RUNNER_TEMP` → `GITHUB_ENV` binding. Scoped `git diff --check` passed with line-ending warnings only. |
| P0 official contract check | GitHub's current context-availability table lists `jobs.<job_id>.env` contexts as `github, needs, strategy, matrix, vars, secrets, inputs` and excludes `runner`; `runner.temp` is runner-local. This independently supports binding `RUNNER_TEMP` inside a running step. |
| P1-A provisional storage gate | New `storage/runtime_control.py`, database initialization seam and temporary-SQLite tests passed `24`; combined with the P0 contract test, `/root` reproduced `26 passed in 1.08s`. No live or default database was opened. This result is not accepted because independent review found missing correctness contracts. |
| P1-A independent review | `REQUEST CHANGES`. Reproduced: lease operations omitted actor/attempt/runtime preconditions; a running parent could spawn a child; caller-owned stale acquire left a partial expiry write; terminal lease-id replay returned a non-active lease; runtime-turn schema lacked restart/process/continuity fields. Fixes and regressions are in progress. |
| P1-A second specialist gate | The first five findings are fixed and `/root` reproduced `40 passed in 1.79s`; combined scoped Ruff passed after removing one P0 test formatting defect. Re-review remains `REQUEST CHANGES`: two different running roots or siblings can coexist for one Actor/attempt, and a delayed heartbeat can move `heartbeat_at`/`expires_at` backwards and permit premature takeover. Both reproductions are now mandatory regressions before P1-A acceptance. |
| P1-A accepted storage gate | Database partial uniqueness now enforces one running turn per canonical Actor, and heartbeat/expiry updates are monotonic CAS operations. `/root` combined P0/P1-A run: `43 passed in 1.25s`; scoped Ruff and diff checks passed. Third independent review ran the P1-A pair as `41 passed` and returned `ACCEPT`, including two-connection roots/siblings, exact replay, terminal handoff, delayed heartbeat/advance, and unchanged-record checks. |
| P1-B initial combined gate (2026-09-01) | `/root` reproduced `278 passed in 9.07s` across physical lock, Chrome lifecycle, durable/in-memory Broker, storage, control-plane, and CI-contract tests; scoped Ruff passed. This is provisional only because independent review found lifecycle combinations absent from authored tests. |
| P1-B1 independent review | `REQUEST CHANGES`. Reproduced: CDP may disappear before the real Edge child/profile holder exits; worker-id-only tracking can overwrite an unresolved Edge generation during Cloak fallback; abandoned/recovery sidecars can be bypassed by one retry; and a fast-exit spawn can leave a lock-only generation outside global cleanup enumeration. Actual browser PID/birth verification, generation exclusion, durable recovery state, and unified cleanup registry are required. |
| P1-B2 independent review | `REQUEST CHANGES`. Reproduced: the façade closes borrowed/thread-local SQLite connections; renewed timestamps make otherwise-current old tokens falsely expire; and multi-row scope release can commit a partial release. Connection ownership, immutable token comparison, and storage-level atomic batch release are required before launcher injection. |
| P1-B2 accepted durable-Broker gate | After two correction rounds, third review returned `ACCEPT`: borrowed connection configuration and outer transactions remain caller-owned; heartbeat-renewed old tokens validate by immutable identity; multi-row scope continuation/release is one exact-token transaction; hidden rows, mixed authority, stale epochs and concurrent advance are all-or-none fail closed. `/root` combined P1 suite reached `293 passed in 10.54s`; scoped Ruff passed. |
| P1-B1 second independent review | The original four findings are closed, including CDP-resolved actual browser PID/birth and persistent recovery state. Review remains `REQUEST CHANGES` on three nested-failure paths: unresolved generations can release their CDP claim too early; cleanup/sidecar failure can lose the lock object while leaving the worker reservation stuck; and ReleaseMutex/CloseHandle/unlink need separate retryable state. |
| P1-B1 third independent review | The nested cleanup and unresolved-generation paths are closed, and `/root` reproduced a broader `299 passed in 9.97s` P0/P1 combination with scoped Ruff green. Review still returned `REQUEST CHANGES`: `release_cdp_port()` could expose a pop/unlink window in which a concurrent same-worker allocation installed a new claim, then a failed old unlink overwrote it. Keep P1-B blocked until release is a generation-bound CAS reservation and the deterministic two-thread race test passes. |
| P1-B accepted profile/Broker gate | A release-generation reservation closed the concurrent unlink/reallocate race; an outer `finally` also guarantees that asynchronous interruption cannot strand the reservation. Fifth independent review returned `ACCEPT` after deterministic race and `SystemExit` helpers. `/root` then reproduced the full P0/P1 combination at `301 passed in 10.42s`; no browser, ATS, default database, commit, or push was used. |
| P1-C durable runtime specialist gate | Durable reservation now precedes `Popen`; PID/birth attachment, checkpoint-confirmed terminal CAS, exact recovery and SubmissionGate continuation, persistent `unknown` admission, receipt-only routing, and pre-reconcile zero-side-effect ordering are production-wired. Focused P1-C composition reached `228 passed`; two real `sys.executable` kill/restart cases passed with every child proven terminated. |
| P1 final combined gate | `/root` ran the isolated P0/P1 shared suite against a dedicated temporary `APPLYPILOT_DIR`: `553 passed in 19.84s`. Scoped Ruff passed. Independent production review reran the launcher/runtime/real-process slice as `27 passed` and returned `ACCEPT`. No browser, ATS, default database, commit, push, or live submission was used. |
| P2 accepted semantic-write gate | Resume-only semantic operations bind attempt, actor, exact page lease/epoch, provider, adapter, material, policy and postcondition; adapter code has no Submit/Next/ledger authority. Exact provider-visible filename and stable acceptance are required, any dispatched attempt blocks legacy or cross-provider second writers, and crash replay requires explicit `failed_no_effect`. Two real OS-process restart tests and real local Chromium synthetic Workday/SmartRecruiters cases passed. Final P0-P2 composition: `414 passed in 20.69s`; scoped Ruff passed; fourth-round independent review returned `ACCEPT`. No real ATS, default database, Submit, receipt, commit, push or remote action occurred. |

## Open risks and remaining work

- External profile migration must not erase user-specific answers or resume bindings.
- Real providers may expose CAPTCHA, assessment, expired listings, historical duplicates, or rate limits.
- The P0/P1 diff is integrated in the sole registered `main` worktree, pushed to `fork/main`, and verified by remote CI run `33304564021`.
- LinkedIn external route persistence is now on the early marker/observer path and remains guarded by the reservation-time gate. The marker is a control-plane transition, not a submission receipt; its no-fill guarantee still depends on the Agent obeying the immediate host-check prompt and the runtime stopping before the next external form action.
- HP Workday resume upload now has connected-browser proof, but the live segmented-date corruption shows the calendar-mandatory rule must be enforced in the adapter/runtime before unattended Workday scaling. Localized yes/no labels remain bounded to confirmed facts and exact visible options.
- Two-worker scaling is currently safe only inside one coordinator with independent preparation pages and a single final submit writer. Multiple launcher processes sharing Edge/Cloak profiles remain unsupported.
- Four-worker production scaling is not yet accepted: no matched 1/2/4 cohort exists, and isolated browser identities cannot reproduce the connected Edge login state. The next scaling gate requires the new acquisition/lane/phase metrics plus unchanged receipt, uncertainty, CAPTCHA/login, upload, and observer quality rates.
- Ordinary first-party ATS sign-in, existing authenticated sessions, configured credential relay, already-signed-in Google basic identity/email SSO, and routine final Submit after all configured gates pass no longer require repeated confirmation. CAPTCHA, MFA/security challenges, recovery, abnormal OAuth scopes, identity/financial material, unsupported material answers, and submission-uncertain states remain separate stop conditions.
- Provider identity normalization is incomplete for Lever URL variants and SmartRecruiters public IDs versus publication UUIDs; both gaps can admit false-new rows unless the visual exact form identity is re-bound before submission.
- SmartRecruiters resume parsing and custom select widgets require post-upload contact verification and value-commit verification; an apparent click is not sufficient evidence that the form consumed an answer.
- Transcript/NTU-proof-required and placement-letter-required roles remain blocked because no exact current artifact exists; optional fields must stay blank and required fields must skip. Exact education start months are now configured. A required FIN/NRIC text field can use the authorized local DPAPI relay, but a required identity-document upload still needs an exact verified artifact; no FIN document was supplied.
- The FIN relay's required-field matching, masking, persistence recheck, and no-submit contract are covered locally, but its display masking has not been exercised against a live ATS framework widget. Any mismatch or failure to persist must fail closed rather than trigger a retry or submission.
- GSK Workday is paused before ordinary account creation. Its validated resume is real, but the cover-letter gate cannot be resolved until the live My Experience step is inspected; no account or draft exists yet.
- The Actor kernel and M3 durable-write contract are genuinely consumed by real `run_job`, control-plane persistence/HumanRequest projection and the bounded pre-submit browser fallback decision. The current slice has stable identity, monotonic CAS and deterministic completion replay, but it still does not start a fresh Agent turn from checkpoint/HumanResponse state; same-task resume, durable exception dispatch and a production `AgentRuntime` adapter remain unimplemented.
- The older remote Python/Windows matrix was green on `d9d6712`, but the current `12d20b6` run created zero jobs and is a release blocker until P0 is pushed and the exact four-job matrix is observed green.
- Stage-specific tests and overall qualification have not yet been completed for the new durable-control-plane continuation. No real provider test, paused-wave resumption, or worker-cap promotion is admitted before those gates pass.

## Happy Pilot P0-P4 wave status

| Phase | Owner task | Current status | Promotion evidence |
| --- | --- | --- | --- |
| P0 baseline and measurement | `Happy Pilot P0 基线与指标` | Complete and accepted locally | `RuntimeNamespace`; shortened submit lane; MCP-ready/direct-child RSS/token metrics; display-only Happy Pilot branding; `664 passed in 32.32s`; scoped Ruff and diff check passed |
| P1 persistent sessions | `Happy Pilot P1 持久会话差距图` | Functional slice complete and accepted locally; production transport/performance admission remains open | Browser reuse across sequential applications; isolated page/actor/session/output ownership; generation-bound restart; default-off loopback HTTP MCP seam; `1688 passed in 192.98s`; scoped Ruff and diff check passed |
| P2 semantic browser | `Happy Pilot P2 语义浏览器差距图` | Functional slice complete and accepted locally; ordinary run path not globally cut over | P1-bound descriptors/capabilities; Workday/SmartRecruiters routine operations; fail-closed frame/shadow/stale/ambiguous/stateful/Submit boundaries; focused `188 passed`; full `1699 passed`; final target binding `12 passed`; Ruff/diff green |
| P3 durable recovery | `Happy Pilot P3 自主恢复差距图` | Complete and accepted locally | Existing-event-store typed command/result; durable episode/evidence; crash-safe effect-unknown; one bounded descriptor diff replan; real non-submit worker seam; full `1708 passed`; Ruff/diff green |
| P4 concurrency promotion | `Happy Pilot P4 并发门禁接管` | Mechanism and local cohort complete; 2/4 workers NOT ADMITTED | 24 tasks × 10 measured blocks per worker count; 720 samples; 2-worker lower bound `1.307x`, 4-worker `1.367x`, both p95 gates failed; all safety/quality/resource/zero-side-effect gates passed; full `1715 passed`; cap unchanged |

Current integration base is `577c6f7de339ba1be4dfe7f18bd4c94fea3c7f22` in `C:\Users\raede\.codex\worktrees\happy-pilot-p0-p4-20260902\ApplyPilot`. The parent worktree remains untouched and intentionally dirty. No phase may run a real provider submission, use the live database/profile, change the production worker cap, commit, push, merge, deploy, or edit these task records.

P0 accepted delivery is an uncommitted worktree diff. Its own scope is 27 files, approximately `+504/-102`, excluding these three root-owned records. The gate used isolated `%TEMP%\hp-p0-*` roots and did not run a browser, provider, mailbox, Submit, or receipt action. `mcp_ready_ms` is the first MCP-call readiness upper bound and RSS is direct-child best effort; process-tree RSS and matched 1/2/4 evidence remain P1/P4 work.

P1 accepted delivery is also an uncommitted worktree diff. One worker can retain one physical browser and endpoint generation while sequential applications receive distinct actors, attempts, sessions, pages, outputs, and Broker bundles. Rolling restart is bounded and `submit_started` forbids restart/switch. Explicit HTTP mode pins `@playwright/mcp@0.0.79`, binds a loopback listener to the recorded child process tree and generation, and renders Codex URL-only MCP config; it remains disabled by default. Fake HTTP/socket and lifecycle tests passed, followed by the isolated full `1688 passed in 192.98s`, scoped Ruff, and `git diff --check`. No package download, real browser/CDP attach, provider, default database, Submit, receipt, commit, or push occurred.

P2 accepted delivery is an uncommitted functional capability seam. It adds `FormSurface`/`ControlDescriptor`, exact P1 actor/attempt/application-session/browser-generation/root-target/page-lease/epoch binding, HMAC TTL single-use authorization, and two-observation postcondition checks before page-epoch CAS. Workday and SmartRecruiters receive only supported routine operations; final Submit, arbitrary file paths, ledger, reservation, receipt, and generic page-write authority remain absent. The focused composition passed `188`, the isolated full suite passed `1699 in 113.65s`, and the post-suite root-target regression passed `12 in 8.23s`; scoped Ruff and diff check passed. The ordinary `run_job` path still uses its existing browser flow and was not globally cut over. No real ATS/default profile/database/mailbox/CDP/Submit/receipt action occurred.

P3 accepted delivery is an uncommitted production-consumed non-submit recovery slice. It persists `ApplicationEpisode` and source references/evidence hashes while keeping commands/results in the existing `agent_events` authority. Command admission is idempotent and crash-safe; an admitted unknown effect parks instead of replaying. One same-application/same-provider/same-lease P2 descriptor diff replan is bounded and consumed before replacement execution. Missing/conflicting material evidence blocks automation but does not block safe human handoff, parking, or receipt reconciliation. P3 focused `8`, P3/recovery composition `10`, corrected isolated full suite `1708 in 119.40s`, scoped Ruff, and diff check passed. No Submit/live provider/default resource/commit/push occurred.

P4 accepted delivery is the concurrency evidence/governor mechanism, not a higher worker-count promotion. The final isolated cohort ran 24 deterministic Workday/SmartRecruiters routine/stale/human/crash-replay jobs across 2 warmup and 10 measured blocks for 1/2/4 workers. Throughput was `4.915/6.512/6.858 jobs/s`; speedup lower bounds were `1.307x` and `1.367x`; p95 ratios were `1.270x` and `2.058x`. Both performance gates failed, so local status is `NOT_QUALIFIED`, production status is `NOT_ADMITTED`, and the governor clamps a 4-worker diagnostic request to 1. SQLite busy, lock wait, submit-lane wait/hold, RSS, quality digest, completion, duplicate/cross-talk/stale/repeated action, and Submit/Gate/reservation/receipt counts all passed. Final focused `7`, cross-phase/operator `70`, isolated full `1715 in 111.91s`, Ruff/format/diff/CLI checks passed. No live/default/remote action or cap/config change occurred.

## 2026-09-03 integration audit status

- [x] Correct display identity to CapyPilot while preserving ApplyPilot technical identifiers.
- [x] Remove the duplicate production `ApplicationEpisode` recovery wrapper; retain the module as diagnostic/P4-only evidence projection.
- [x] Remove production `SemanticBrowserOps.apply_control`; retain exact semantic control execution only in the isolated no-submit P4 worker and tests.
- [x] Use current Windows working set for application-boundary recycle decisions and cover current-below-limit/peak-above-limit behavior.
- [x] Keep loopback HTTP MCP default-off and keep production workers at 1.
- [x] Post-audit focused suite: `282 passed in 20.39s`; scoped Ruff passed.
- [x] Final combined CapyPilot/P0-P4 gate: `1721 passed in 116.40s`; full Ruff and `git diff --check` passed; audited release build produced wheel, sdist, bundle, and checksums; clean-wheel smoke passed install, `resume-route --help`, `CapyPilot 0.4.0`, init, doctor, and dashboard generation.
- [x] Ordinary push to `fork/main`; exact-head CI run `33708579050` scheduled four non-empty jobs and all passed on `cc14585` (Python 3.11/3.12/3.13 and Windows 3.12 clean-install smoke).
- [x] Fast-forward local `main`; remove the two isolated worktrees, their three local integration/source branches, and the merged remote hardening branch. Preserve stash `027f5756` because 26 non-temporary paths are not byte-identical to final `main` and therefore are not safe to discard as redundant WIP.

## 2026-09-04 remaining-architecture status

- [x] Merge and push the round-two P0-P4 branch to `fork/main` at `2d1323e`.
- [x] Verify local HEAD, tracking ref, remote ref, and GitHub Actions exact-head
  success across five non-empty jobs.
- [x] Create the isolated `codex/remaining-architecture` integration worktree.
- [ ] R1 authoritative App Server prepare canary: `off|shadow|canary` is
  configured and canary runs against real traffic as a bounded observation,
  while a separate one-shot prepare-only capability contract is now reviewed
  and green. CLI remains authoritative because no production browser-write
  adapter or durable takeover handoff is admitted.
- [x] R2 host-owned ApplicationPlan plus deterministic host audit; the existing
  lease-bound SemanticPatchBatch path remains authoritative.
- [ ] R3 fresh BrowserContext and Provider recipe cutover: context isolation and
  separate Greenhouse/Workday/SmartRecruiters shadow observations are green,
  but production browser writes and recipe replay remain disabled.
- [x] R4 matched cohort and Runtime Cell admission decision: schema v3 uses the
  status/expiry index and the current-source Playwright A/B/C cohort completed.
  All cleanup and zero-effect gates passed, but A/B/C wall time was
  `2812/3360/4100 ms`; effective production capacity remains one
  (`NOT_ADMITTED`). Application acquisition still performs two candidate passes;
  the new counters measure this blocker but do not claim it is fixed.
- [x] R5 local combined/full/release/no-submit qualification: Runtime focused
  union passed `213`; final full suite passed `2211`; full Ruff and diff checks
  passed; wheel/sdist and clean-wheel CLI smoke passed; real App Server and
  Edge/CDP smokes passed with clean shutdown. The exact Workato dry-run again
  stayed at two historical attempts, zero submit-started attempts, zero runtime
  turns and zero receipts because stale material exhausted the manifest before
  browser launch.
