# Context

## Current truth

- On 2026-08-31 the user supplied exact education start months, authorized local encrypted FIN use only when a required application field cannot be left blank, and confirmed that unavailable NTU/programme-placement proof is optional-blank or required-skip. The FIN path is now production-consumed and fail-closed across exact attempt/application/page/required-field binding, pre-decrypt checks, one-shot DPAPI use, display masking, page/audit redaction, and durable-report rejection. scikit-learn, TensorFlow, PyTorch, experimental design, and A/B testing are ongoing, low-weight application-fact keywords only—not completed outcomes, production maturity, skills-boundary entries, or default cover-letter claims.
- On 2026-08-30 the user paused the live wave for the day at `5/6`. NCS AI Engineer Intern is receipt-confirmed and reconciled. GSK Workday req `444672` is stopped at step `1/7` before account creation, so no GSK account or draft exists; NCS Business & Strategy remains the validated fallback. No live application process is running.
- On 2026-08-30 the user explicitly started a new real-world test targeting six applications from different library channels and restated broad routine authorization. This is authority to execute admitted applications, not authority to bypass CAPTCHA, assessment, identity/financial, unsupported-answer, platform hard-gate or uncertain-receipt boundaries. Current profile permits up to ten standing-authorized submissions per run, but this test is capped at six confirmed receipts and one live worker.
- The second shared-chat review was read from the exact user-mentioned signed-in Edge tab. It independently confirms that `436bf2a` is a green P0/P1 baseline and that the Actor is a real but bounded recovery-gate slice, not yet a durable Actor loop. Repository inspection reproduced the key gaps: `run_id` is still generated per `run_job`, completed-turn checkpoints use fixed sequence `1`, and production does not consume `latest_checkpoint` for resume.
- The M3 wave completed before M4/M5 or worker promotion. User-visible task `01a052a3-5706-7531-8f54-c8eb9aea96d7` delivered durable Actor identity/checkpoint changes; user-visible task `01a052a3-5d0a-71a3-96ae-4b006374c140` delivered the Windows CI and installed-CLI truth gate. They preserved disjoint ownership and performed no commit, push or live application run.
- On 2026-08-30 the broader integration checkout originally started at `1da96ad3aa57e147226e4e7c9d54f0fc4dff638f`; the M3 wave itself started clean at `436bf2abc7c963d0d9ac645ba525832b8b6bde9a`, tracking `fork/main`. The existing provider-neutral contracts, DAG coordinator, durable ledgers, submission gates, single-writer controls and telemetry were extended rather than recreated.
- GitHub Actions run `33296366807` exposed the same three hermetic failures on Python 3.11/3.12/3.13. After commits `7e928e7aeb581163014274036b2ad217e407973d` and `d9d67129e703ee4ffee6d4236720e7af23ec7dac` were pushed to `fork/main`, run `33304564021` passed Python 3.11/3.12/3.13 and Windows clean-install smoke.
- The shared ChatGPT conversation recommends an Application Actor, typed autonomy/recovery, session/profile pools, higher-level browser tools, one canonical structured result, exception parking, and measured pipeline concurrency. Current inspection confirms the direction but also shows that contracts, journals, scheduler primitives, single-writer gates, and telemetry already exist.
- Two user-visible Codex tasks completed the first slices: `01a051b6-3da7-7db1-9a4c-379847e5c34d` repaired the three hermetic test seams, and `01a051b6-6fef-7d71-88a3-2836d602f2ec` implemented the Actor/Policy/Recovery vertical slice. `/root` reviewed the overlap and ran the final combined validation.
- The final M3 working tree passes `955` tests and full Ruff. The Actor is not a dead module: real `run_job` writes schema-v2 actor/turn identity, actor-scoped CAS checkpoints and typed HumanRequests through the existing control plane, while the worker still consumes `retry_new_session` before the established one-shot Edge-to-Cloak fallback. It has no browser, ledger, page-write, receipt or Submit authority, and no production fresh-turn resume consumer exists yet.
- Historical implementation worktree: `C:\Users\raede\.codex\worktrees\apply-p0-p2-20260829\applypilot`, branch `agent/p0-p2-performance-orchestration-20260829`, initially clean at `d53ae6e`; it is not part of the current registered-worktree set.
- Current integration worktree: `C:\Users\raede\Desktop\简历\applypilot-local\source`; all present changes belong to this P0/P1 wave, and no unrelated dirty paths were found.
- Runtime data root for supervised tests: `C:\Users\raede\Desktop\简历\applypilot-local\data`.
- LinkedIn is modeled as three explicit paths: `linkedin_apply_entry`, `linkedin_native_easy_apply`, and `linkedin_to_official_ats`. The entry path can resolve to native Easy Apply or a verified current-job official/ATS handoff. A same-attempt external continuation now requires target lineage, recognized/trusted ATS, bounded title/company identity evidence, attempt-bound runtime binding, and provider identity where required; the shared manifest is never mutated.
- External handoff observation no longer depends on the Agent emitting `RESULT:LINKEDIN_EXTERNAL_HANDOFF`. The worker records the initial LinkedIn entry, observes the bound page after the first turn, and can override a mistaken pre-submit result only when the launcher independently verifies the exact external route. Unknown, ambiguous, or wrong-job targets fail closed and never count as submission evidence.
- Earlier supervised tests added zero receipt-confirmed applications. The current six-channel wave has since produced five distinct, reconciled receipts: ShopBack Product Management, ShopBack Backend Engineering, Simular AI Product Manager, Grab OmniCommerce, and NCS AI Engineer Intern. NCS receipt `browser-21653a79-ecbf-406b-90dc-2ab048cd0d67` is bound to the corrected SmartRecruiters publication UUID and exact database row. A prior two-worker fill-only dry-run still created no batch reservation or receipt and remains performance evidence only.
- The runtime profile explicitly and separately authorizes ordinary first-party ATS sign-in, configured credential relay, existing Google-session reuse, ATS account creation, and narrow matching-employer Gmail verification. These ordinary login actions do not require a separate per-login confirmation. On LinkedIn itself, the prompt requires one bounded Continue-with-Google attempt, an exact return to the same job ID, and one exact primary-Apply retry. ATS account creation remains first-party only; LinkedIn is not authorized as a third-party ATS OAuth provider. Routine final Submit is now standing-authorized after exact-job/material/preflight/submission gates pass; CAPTCHA, MFA or identity-provider security challenges, recovery, abnormal scopes, identity, financial materials, unsupported material answers, and submission-uncertain states remain hard stops.
- The final isolated regression passes `876` tests in `54.51s`; full-repository Ruff and `git diff --check` pass. The latest focused orchestration/runtime/acquisition/attempt-ledger slice passes `344` tests, and the LinkedIn page-observation slice passes `34`. Independent reviews accept both the final LinkedIn source-identity gates and the acquisition/lane performance telemetry.
- Production material-readiness proposals now execute through a durable journal and deterministic reducer. Live execution and replay behavior remain to be measured in this task.
- A forensic review traced the LinkedIn exclusion to a one-batch request to prefer direct official sites being copied into the global external profile. The same prior design retained LinkedIn support, so this is a policy-scope regression rather than an intentional product removal.
- The shared admission predicate now reports one executable row from the current runtime database. Attempt-exhausted and retry-blocked rows remain fail-closed instead of inflating the ready count.
- LinkedIn pre-entry authwall is now admitted only when one direct root target redirects, after exactly one query decode, to the same exact LinkedIn job. The authwall never becomes an application URL or external ATS route. A separate exact `cta-modal` app-promo dismissal is followed by two source target/root/job revalidations before Apply; independent review accepted the closed race fix.
- Isolated Edge and Chrome profile clones both reached Google identifier login and returned `LOGIN_ISSUE`; they could not reuse the live Google/LinkedIn identity. The Codex App connected Edge session was already signed in, opened exact LinkedIn job `4455274411`, followed `Apply on company website`, reused the existing HP Workday account through Edge-saved credentials without reading the password, saved all six steps, and uploaded the exact machine-validated resume. The final Submit was clicked exactly once; HP showed `已提交申请` / `您的申请已成功提交`, redirected to `/jobTasks/completed/application`, and listed `UNI4131` as `In Progress`.
- The official HP ATS showed `UNI4131-1` as an unsubmitted draft while the Simplify extension claimed an Aug 30 application. Treat this third-party extension state as non-authoritative: only the target ATS and durable receipt evidence can establish submission.
- Acquisition and submit-lane performance are now observable without a schema migration or behavior change. Every acquisition attempt, including empty/contended results, has its own denominator; each terminal attempt receives final bounded lane timings only after the writer lane is released; `RunProgress` separately aggregates acquisition attempts and completed-job samples. Production worker count remains capped at the currently supported two-worker coordinator until matched 1/2/4 cohorts show a quality-preserving gain.

## Decisions and deviations

| Time | Evidence or decision | Impact |
| --- | --- | --- |
| 2026-08-29 | Use the existing clean P0-P2 worktree instead of editing the dirty parent. | Protects user and agent WIP; all implementation and static validation occur in isolation. |
| 2026-08-29 | Keep one writer per application page and parallelize only independent applications or read-only frozen-snapshot work. | Concurrency work cannot weaken submit ownership or receipt gates. |
| 2026-08-29 | Do not treat Agent output, screenshot names, Submit clicks, historical duplicates, or uncertain pages as new receipts. | Live-test success remains fail-closed. |
| 2026-08-29 | Focused baseline passed: `118 passed in 5.27s` using the parent virtual environment with this worktree's `src` on `PYTHONPATH`. | Establishes the isolated checkout matches the pre-change orchestration/runtime contracts. |
| 2026-08-29 | Official npm registry reports `@playwright/mcp` latest/stable `0.0.79` and `@gongrzhe/server-gmail-autoauth-mcp` latest `1.1.11`. | Pin Playwright instead of using `@latest`; only pin Gmail if its command path is changed in this scope. |
| 2026-08-29 | Post-submit evidence slice completed with six focused tests plus the 84-test runtime contract suite. | Historical duplicates become permanent `already_applied`, remain `confirmed=false`, never retry/repair, and never increment new applications. |
| 2026-08-29 | Independent evidence review returned `REQUEST CHANGES`: bare `already applied` can override a decisive new receipt, and legacy screenshot aliases can mark non-success retention metadata as `applied`. | Add conflict classification and explicit receipt-admitted retention metadata; passing focused tests alone is not sufficient. |
| 2026-08-29 | Policy probe showed an explicit malformed surface policy fails open and `maximum_apply_attempts` is not yet consumed; a generic LinkedIn external target is classified as LinkedIn-to-ATS. | Harden malformed policy and attempt configuration before migrating the runtime profile; retain a documented trust boundary for generic external targets. |
| 2026-08-29 | Canonical policy hardening passed 243 targeted tests; malformed policy, unknown external LinkedIn targets, exhausted attempts, and all retry blocks now fail closed. | Authorization, queue selection, and runtime acquisition use the same admission policy. |
| 2026-08-29 | Independent orchestration review found the material enforce result was not consumed, conditional “if you have already applied” text could become a false permanent duplicate, and material replay identity omitted decision inputs. | All three findings were fixed with worker-boundary and replay regression tests before live execution. |
| 2026-08-29 | Independent performance review found the scheduler unused and the final submit/observer window lacked a held single-writer permit. | The launcher now consumes the execution plan; a process semaphore and database active-writer guard serialize final submission through durable outcome. Multiple launcher processes remain outside the supported live-test boundary. |
| 2026-08-29 | User clarified that LinkedIn Apply frequently redirects to employer sites and does not require Easy Apply. | Added `linkedin_apply_entry`; verified external handoffs are rebound to a fresh exact manifest instead of being ignored or submitted under stale authorization. |
| 2026-08-29 | The first full-suite run inherited the live standing-authorization profile. A CLI rejection test therefore entered a real Infineon prepare attempt before the run was interrupted. Durable evidence showed `phase=prepare`, `submit_started=0`, no gate, no consumption, and no receipt. | Recovered the sole attempt through the stale pre-submit protocol as `abandoned_pre_submit`; changed the test to inject an isolated no-authorization profile; all later suites use a dedicated temporary data root. |
| 2026-08-29 | Live preparation revalidation rejected Grab resume bytes for unsupported/inflated claims, while the resume library still offered the same bytes as a validated reusable artifact. | Keep Grab blocked and invalidate same-content artifacts after a failed strict revalidation before any route projection or browser run. |
| 2026-08-29 | CLI `status` showed eight raw prepared rows while canonical admission admitted only two after HP and Shopee routing. | Preserve the legacy raw pipeline count but display a separate `Admission-ready (pre-manifest)` canonical count and the remaining manifest/runtime caveat. |
| 2026-08-29 | Supervised HP run clicked LinkedIn Apply, reached Workday, used credential relay, and attempted resume upload twice, but failed pre-submit after 228 seconds. The external URL was not persisted because route resolution ran only at final reservation. | Move trusted external handoff detection ahead of ATS form filling so stale-manifest work stops immediately; then rebind and test the exact Workday page separately. |
| 2026-08-29 | LinkedIn early-handoff checkpoint was added across prompt, result marker, page observer, route gate, and worker reducer. The three LinkedIn paths are now distinguishable; trusted ATS/official targets are sanitized and rebound, while the current stale-manifest attempt is deferred with zero reservation. | Treat `LINKEDIN_EXTERNAL_HANDOFF` as a control-plane transition, never as `applied`; keep final reservation-time route verification as a second gate. |
| 2026-08-29 | Controlled two-worker dry-run was measured with one coordinator. Both workers were allowed to prepare independent fill-only previews, while the submit writer stayed serialized at capacity one; the run created zero receipt-confirmed applications. | Parallelize preparation only; do not start multiple launcher processes against shared browser profiles or treat worker count as submission concurrency. |
| 2026-08-29 | Runtime telemetry now records `process_spawn_ms`, `turn_setup_ms`, `prompt_build_ms`, `first_output_ms`, `first_tool_ms`, `last_tool_ms`, `tool_call_count`, and `unique_tool_count`; the two-worker run was slower than the single-worker baseline. | Use these fields to compare orchestration overhead and provider latency separately; no performance claim is promoted from fixture replay alone. |
| 2026-08-29 | Fill-only preview semantics were verified: preview may populate and inspect the real form but must not click Submit/Send/Finish, send email, or create a receipt; preview state is restored and `previewed` is not counted as applied. | Keep preview, authorization, final action, and receipt-confirmed application counts separate. |
| 2026-08-30 | v3 Edge history proved the exact LinkedIn job was redirected to a same-job `/authwall` before any Apply click. A bounded runtime login-entry classifier and dry-run evidence preservation were implemented. | Login entry, launcher-owned Apply click, and external ATS handoff remain separate causal stages. |
| 2026-08-30 | v4 exposed a non-login `LinkedIn is better on the app` overlay. Exact dismissal fixed the live timeout; independent review then found and closed a post-Dismiss job-drift race with two exact source revalidations. | Normal popup handling no longer weakens wrong-job fail-closed behavior. |
| 2026-08-30 | The connected Codex App Edge session traversed LinkedIn job `4455274411` to HP Workday `UNI4131-1`, while isolated browser clones could not reuse the same identity. | The core route works; the remaining tool gap is an authenticated connected-browser runtime adapter, not an Easy Apply-only policy. |
| 2026-08-30 | Performance review found Agent turns and the wide single-writer lane dominate scaling risk. Bounded acquisition/lane/phase metrics and run aggregation were added before any concurrency increase. | Keep two workers; measure matched 1/2/4 cohorts before changing production capacity or narrowing the lane. |
| 2026-08-30 | The user granted standing authorization for ordinary ATS sign-in, configured credential relay, and reuse of an already authenticated Google/browser session. The private profile records that authority, while versioned policy separates it from account creation, material declarations, and final submission. | Do not pause merely because an ordinary login page appears; continue one bounded login attempt, but retain CAPTCHA/MFA/recovery/abnormal-scope/identity/financial and unsupported-answer stops. |
| 2026-08-30 | Connected Edge completed HP Workday login, saved steps 1-4, and reached Review (6/6). The user confirmed `No` for the five-year government/public-agency-employment question: internships without a staff appointment/establishment do not count under the user's stated answer policy. The user also confirmed `No` for the exact conflict/government-influence questions shown. | Apply these facts only to those exact questions; do not infer answers to broader employment, authorization, family, legal, or other screening questions. Final Submit remains unclicked and no receipt exists. |
| 2026-08-30 | HP's official ATS showed `UNI4131-1` as an unsubmitted draft, although Simplify displayed “Applied on Aug 30.” | Third-party extension status is not a receipt and must never override target-ATS state or durable receipt evidence. |
| 2026-08-30 | Edge restored the existing Workday session through saved credentials; no password was inspected or exposed. | Ordinary credential relay stays bounded to the browser's saved-login flow and does not relax material-answer or final-submit gates. |
| 2026-08-30 | The Workday live page showed that segmented date typing can lose focus and corrupt the year to `2031`; calendar navigation restored the intended date. | When a Workday calendar is available for a segmented/composite date, it is mandatory. Do not use the typed per-segment fallback on that control; retain the confirmed-fact gate and verify the resulting visible value. |
| 2026-08-30 | The user explicitly authorized routine final Submit without repeated confirmation after all configured gates pass. HP `UNI4131` was then submitted exactly once; the official confirmation page and candidate portal were reconciled as browser receipt `hp-workday-UNI4131-completed-application-20260830T1029+0800`. | Future routine submissions proceed without another consent prompt, while CAPTCHA/assessment/identity/financial/unsupported-answer/submission-uncertain boundaries remain unchanged. |
| 2026-08-30 | Reconcile the shared-chat proposal against current main before implementation. Existing control contracts and persistence are reusable; the real gaps are hermetic CI, production Actor adoption, typed recovery/interruptions, and durable session/browser leases. | Use a strangler migration with shadow comparison; do not duplicate the already-implemented P0-P2 orchestration primitives or begin a framework rewrite. |
| 2026-08-30 | Split the first execution wave into two user-visible tasks with disjoint ownership. | P0 changed only the three failing-test seams. P1 required two narrow non-submit consumers: launcher persistence/HumanRequest projection and worker fallback gating. This bounded deviation is accepted because both consume the same envelope, while browser, ledger, Submit authority, docs and live application execution stayed out of the task. |
| 2026-08-30 | The user rejected scaffold-only delivery and required proof that implemented recommendations are consumed by real Agents and code. | P1 scope was tightened to require one low-risk production vertical slice: real entry invocation, reducer/policy/state consumption, a control-flow effect, and integration tests. A module that is merely importable or telemetry-only remains `PARTIAL`, not landed. |
| 2026-08-31 | Education starts, unavailable-proof handling, FIN authorization, and ongoing ML/experiment keywords were converted from user notes into production-consumed profile and runtime contracts. | Render dates in the real Agent prompt; leave optional evidence blank and skip when unavailable evidence is required; expose the encrypted FIN only through a host/lineage-bound required-field relay with no Submit authority; keep ongoing framework/method terms at keyword-only weight and never infer completed or production outcomes. |
| 2026-08-31 | Independent security review found that host/root binding alone could cross postings and that label-only/full-value-only redaction was incomplete. | Bind FIN authorization to a digest-protected exact attempt/application route and provider identity, require one exact required field before decryption, consume authorization once, verify and mask after fill, redact explicit markers and identifier metadata, and reject protected identifiers even when embedded in report text. Security re-review passed; real ATS widget behavior remains a separate live-smoke boundary. |
| 2026-08-31 | Cover letters were being generated speculatively for every tailored high-fit job even though the live ATS path can determine whether a letter is required; LinkedIn Applied already accumulated history in `jobs`, but every fresh report gate still conflated a full snapshot with a small update. | The full pipeline now opts out of speculative cover generation by default while explicit generation and runtime-required generation remain available. LinkedIn sync now distinguishes complete baselines from incremental ledger patches, rejects duplicate IDs and non-LinkedIn lineage, and never lets an incremental patch satisfy the complete/fresh radar gate; periodic full validation remains required because no repository-visible official cursor proves incremental coverage. |

## Live process ownership

| Process | Owner | Log path | State |
| --- | --- | --- | --- |
| Focused/unit tests | `/root` | Codex command output; exact commands recorded below | Focused and 407-test combined suites passed; first full run exposed test isolation and was stopped; isolated full rerun pending |
| Controlled multi-worker smoke | `/root` | Codex command output; single coordinator only | Two-worker fill-only dry-run completed; no receipt or batch consumption |
| Supervised real application run | `/root` | `data/logs/application-evidence` plus durable DB outcome | Multiple supervised attempts completed; receipt-confirmed applications: `1`; HP `UNI4131` is `applied` with a reconciled browser receipt. |
| LinkedIn → HP Workday marker-independent smoke v2 | `/root` | `C:\Users\raede\AppData\Local\Temp\applypilot-linkedin-route-live-v2-99345495c93845b284a9c8deecfd4a9d\logs` plus its isolated `applypilot.db` | Completed fail-closed: public LinkedIn Apply opened the login dialog, but the Agent stopped before the authorized Google login. No external route, submit action, or receipt. |
| LinkedIn → HP Workday proof/login smoke v3 | `/root` | `C:\Users\raede\AppData\Local\Temp\applypilot-linkedin-route-live-v3-4acc7306c12a42fa8883807c08342db2\data\logs` plus its isolated `applypilot.db` | Completed fail-closed after the bounded authwall login-only turn returned `LOGIN_ISSUE`; no second Apply, form, submit, or receipt. Preview attempt preserved the failure evidence. |
| LinkedIn → HP Workday proof/login smoke v4 | `/root` | `C:\Users\raede\AppData\Local\Temp\applypilot-linkedin-route-live-v4-cc00b860b4a74dd0a20e840dbc1a79c1\data\logs` plus its isolated `applypilot.db` | Exact app-promo dismissal and pre-entry login dialog classification passed. The isolated Chrome clone still stopped at Google identifier with `LOGIN_ISSUE`; no submit or receipt. |
| Connected Edge LinkedIn → HP Workday | `/root` | Codex App connected Edge tab plus `data/receipts/workday-hp-uni4131-20260830.json` | Exact signed-in LinkedIn job and company-site CTA reached exact HP requisition `UNI4131-1`; existing-account login used Edge-saved credentials without password inspection. Submit ran once, HP confirmed success, and the exact LinkedIn job row now has `durable_receipt_reconciled`. |
| P0 CI baseline repair | Codex task `01a051b6-3da7-7db1-9a4c-379847e5c34d` | Task commentary/final output; no shared live process authorized | Completed; isolated baseline `905 passed`, Ruff passed, and remote matrix run `33304564021` passed. |
| Git integration and push | `/root` | `main` -> `fork/main`; no force and no `origin` mutation | Commits `7e928e7` and `d9d6712` pushed by ordinary fast-forward; code-bearing head CI passed. |
| Actor/Policy/Recovery vertical slice | Codex task `01a051b6-6fef-7d71-88a3-2836d602f2ec` | Task commentary/final output; no browser or live application authorized | Completed; `106 passed` focused gate plus independent contract review. |
| Final combined local validation | `/root` | Stable final working tree; no browser/data-root/live provider access | Completed; `947 passed in 33.50s`, full Ruff passed, `git diff --check` exit 0. |
| Pre-push integration gate | `/root` | `.venv` test process and local package cache only; no ports, live database, browser, ATS, output artifact, or checkpoint | Completed from the staged Actor/P0 integration tree: `947 passed in 31.57s`; full Ruff passed. No retry was needed. |
| M3 durable Actor/checkpoint implementation | Codex task `01a052a3-5706-7531-8f54-c8eb9aea96d7` | Shared checkout; production/control-plane files and focused isolated tests only | Completed: `65 passed`; scoped Ruff/diff checks passed. Independent review found and verified the external-transaction SAVEPOINT repair. No browser, live data, commit or push. |
| Windows core CI and installed CLI smoke | Codex task `01a052a3-5d0a-71a3-96ae-4b006374c140` | Owner-only task output; Windows Python 3.12, `python -m pytest -q`, isolated `APPLYPILOT_DIR=C:\Users\raede\AppData\Local\Temp\applypilot-ci-pytest-01a052a3`; no ports/browser/ATS/live DB | Completed: `947 passed in 30.80s`; YAML/AST checks and isolated wheel smoke passed, including the installed `applypilot.exe resume-route --help`. Temporary directories were removed. Remote Actions remains unproven until a later authorized push. |
| M3 final integration gate | `/root` | `.venv` with isolated `APPLYPILOT_DIR=C:\Users\raede\AppData\Local\Temp\applypilot-m3-final-01a052a3`; isolated release output under `C:\Users\raede\AppData\Local\Temp\applypilot-m3-release-01a052a3` | Completed: `955 passed in 32.12s`, full Ruff, YAML parse and `git diff --check` passed; final wheel build and clean-install smoke passed, including installed `applypilot.exe resume-route --help`. No ports, browser, ATS, live database, commit or push. Cleanup of the two explicit temp roots was attempted but blocked by the command policy, so those disposable roots remain. |
| Six-application multi-channel live wave | `/root` | `C:\Users\raede\Desktop\简历\applypilot-local\data\logs\six-channel-live-20260830`; live `applypilot.db`, connected visual browser, per-turn Agent logs and durable receipts | Paused at user request with `5/6`: two Lever, one Ashby, and two SmartRecruiters receipts are reconciled. NCS AI succeeded once and its exact receipt is durable. ShopBack Operations Automation (2026-08-24) and Business Analyst (2026-08-26) are historical duplicates and uncounted. GSK req `444672` is prepared but stopped before Workday account creation; NCS Business & Strategy is a machine-validated fallback. No process is running and no other owner may resume, retry or stop this wave. |

Shared resources: parent `data/applypilot.db`, `data/logs/application-evidence`, application manifests, Edge/Cloak profiles, CDP ports, MCP package cache, and provider-side application state.

Success condition: target checks pass, live workers terminate cleanly, no cross-talk or duplicate submission occurs, and only durable exact receipts count as applied. Failure/stop conditions: repeated same failure three times, unexpected shared-resource ownership, unsupported material answer, CAPTCHA/assessment/identity/financial/MFA gate, or ambiguous post-submit identity. Test processes must use the dedicated temporary data root, never the live standing-authorization profile.

## Handoff

- `/root` owns scope, integration, shared runtime tests, and final status.
- Subagents have explicit disjoint file ownership. They may run only their narrow static/unit tests; `/root` remains the sole live-process owner.
- The two user-visible tasks must stop before commit/push and report exact changed files. `/root` will inspect overlap and decide whether any production-path integration is admissible.

## Next step

When the user resumes, continue serially from `5/6`. Prefer the different-channel GSK Workday req `444672`: use the configured ordinary-account DPAPI relay, inspect the live My Experience step to resolve whether a cover letter is required, and proceed only if the material gate becomes complete. The education-month blocker is resolved. Optional NTU/programme-placement proof stays blank; if either is required, skip. A required FIN/NRIC text input may use the separately authorized DPAPI identity relay, but any identity-document upload requires the exact verified artifact and otherwise skips. If GSK hard-stops, use the already validated NCS Business & Strategy publication as the fallback. Preserve CAPTCHA, assessment, MFA/security, unsupported-answer, and uncertain-receipt stops; count only one new decisive exact receipt.
